-- Contains engine configurations.
config = {
    title = "MyLittleProject",
    fullscreen = false,
    vSync = false,
	antiAlias = 8,
    resolution = {
        window_width = 800,
        window_height = 600
    }
}
